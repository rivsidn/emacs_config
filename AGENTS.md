该仓库为 Emacs 配置文件。

## 目录约定

| 目录、文件名 | 说明                                            |
|--------------|-------------------------------------------------|
| `file-type/` | 存放按文件类型区分的脚本                        |
| `path-type/` | 存放按特定路径区分的脚本，脚本入口为 `index.el` |
| `doc/`       | 项目需求说明文档，文档入口为 `SUMMARY.md`       |

- 脚本加载顺序为，先加载`file-type`，再加载`path-type`。

## 安装约束

- 仓库通过 `stow` 安装到 Emacs 配置目录。
- 如果新增文件或目录不需要安装到 `~/.emacs.d`，需要同步添加到 `.stow-local-ignore`。

## 实现约束

- 修改 Emacs Lisp 代码之后，需要执行 `make format`。
- 修改脚本实现时，如果涉及功能行为、加载路径、命令名称或配置入口，需要同步更新对应需求文档。

## 文档约束

- `doc/SUMMARY.md` 是项目文档入口；新增或调整 `doc/` 下的文档时，需要同步维护该文件。
- `doc/` 下的需求文档与 `file-type/`、`path-type/` 下的脚本实现存在对应关系。
